# SLO / SLI

SLIs: p50/p95/p99 lifecycle latency, throughput, error rate, concurrency/saturation, dependency health, promotion success rate and rollback rate.

Baseline target: p95 lifecycle operation < 500 ms; transition error rate < 1%; audit evidence complete for promotions.

Reference target; calibrate with production traffic.